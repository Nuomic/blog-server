const { Router } = require('express');
const getArticle = require('./getArticle');
const saveArticle = require('./saveArticle');
const deleteArticle = require('./deleteArticle');
const router = Router();
// const filter = { password: 0, _v: 0 };
module.exports = router;
router.get('/list', (req, res) => getArticle(req, res));
router.post('/save', (req, res) => saveArticle(req, res));
router.post('/delete', (req, res) => deleteArticle(req, res));
